public static boolean isMainPassport(com.arabbank.model.entity.golden.Document document) {
		return document.getType() != null
				&& document.getType().getCode() != null
				&& document.getType().getCode().equalsIgnoreCase("01")
				&& "1".equals(document.getUsage());
	}
