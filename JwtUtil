   @Autowired
    private MockMvc mockMvc;

    @Autowired
    private FileReader fileReader;

    @Mock
    private TaskRepository taskRepository;

    @Mock
    private SubTaskRepository subTaskRepository;

    @Mock
    private SubTasksResponseRepository subTasksResponseRepository;

    @Mock
    private UserActionRequestDto userActionRequestDto;

    @Mock
    private PersonIdentifierDto personIdentifierDto;

    @InjectMocks
    private CheckerController checkerController;

    @Spy//create real object by default call real methods
    //create an instance of the class under test (create real class under test)
    //with dependencies injected automatically

    private CheckerService checkerService;

    @Mock
    private Task testTask;

    @Mock
    private SubTask subTask;

    @Mock
    private MasterPerCountryRepository masterPerCountryRepository;

    @Mock  //is fake implementation,all methods return defauls values(null,0,false,empty collection)
    //doesn't call real methods
    private RelationshipRepository relationshipRepository;

    //run before each test
    @BeforeEach
    public void setup() throws Exception {
        MockitoAnnotations.openMocks(this);
        mockMvc =  MockMvcBuilders.standaloneSetup(checkerController).build();
    }
