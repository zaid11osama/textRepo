try (MockedStatic<HelperUtils> mocked = Mockito.mockStatic(HelperUtils.class)) {
    mocked.when(HelperUtils::generateReference).thenReturn(reference);

    NewPartyModel result = partyService.getNewPartyModel(headers, partyId);

    assertNotNull(result);
}
